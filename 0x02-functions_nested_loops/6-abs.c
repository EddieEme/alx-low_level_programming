#include <stdio.h>
#include <inttype.h>
#include "main.h"
/**
 * _abs - function that computes the absolute value of an integer
 * @n: n is an argument
 * Return: Always 0 (success)
 */
int _abs(int n)
{
	return (abs(n));
}
