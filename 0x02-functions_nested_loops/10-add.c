#include <stdio.h>
#include "main.h"
/**
 * add - function that adds two integers and returns the result
 * @a: first Parameter
 * @b: second parameter
 * Return: Always 0 (Success)
 */
int add(int a, int b)
{
	return (a + b);
}
