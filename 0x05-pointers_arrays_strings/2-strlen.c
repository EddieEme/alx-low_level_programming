#include <string.h>
#include "main.h"

/**
 * _strlen - function that returns the length of a string
 * @s: parameter
 * Return: Nothing
 */

int _strlen(char *s)
{
	int len;
	len = strlen(s);

	return (len);
}
