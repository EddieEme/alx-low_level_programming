#include <ctype.h>
#include "main.h"

/**
* mul - multiply two numbers a and b
* @a: a is first argument
* @b: b is second argument
* Return: Always 0 (Success)
*/
int mul(int a, int b)
{
	int sum;

	sum = a * b;

	return (sum);
}
